import CommentItem from '../commentItem/CommentItem';

class InfiniteLoadScroll extends React.PureComponent {
  static defaultProps = {
  }

  static propTypes = {
  }

  constructor(props) {
    super(props);
    this.displayName = 'InfiniteLoadScroll';
    this.state = {
    };

  }


  render() {
    console.log('%c infiniteList', 'green');

    return (
      <div>

      </div>
    );
  }
}

export default InfiniteLoadScroll;
